# 团队共享文件

这是加入团队协作后自动出现的数据源，对应主机的"团队共享文件夹"。

- 无需手动配置：加入团队（左下角"团队协作"，输入 4 位协作码）时自动连接；退出团队时自动移除。
- 列表里显示的是主机共享文件夹中的数据文件（csv / tsv / xlsx / json / parquet），点击即可加载分析。
- 上传文件到共享文件夹：打开"团队协作"面板，使用"上传到团队共享"。
- 所有传输仅在局域网内进行；访问凭证是加入时获得的成员令牌，主机可以随时移除成员。
